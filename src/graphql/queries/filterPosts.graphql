query filterPosts($searchString: String) {
  filterPosts(searchString: $searchString) {
    id
    title
    content
    published
    author {
      id
      name
      email
    }
    authorId
  }
}
